name: ltb-lawtoolanista
metadata:
  template: ltb-lawtoolanista@1.0.0

services:
  backend:
    project: ./backend
    language: python
    host: function

  frontend:
    project: ./frontend
    language: ts
    host: staticwebapp
    dist: dist

infra:
  provider: bicep
  path: infra
  module: main
